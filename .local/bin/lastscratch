if [[ "$1" == "" ]]; then
    lastscratch=`find $HOME/.vimscratch/ -type f | sed 's/://g' | tail -n 1`
else
    lastscratch=`find $HOME/.vimscratch/ -type f | tail -n $1 | head -n 1`
fi
cat $lastscratch
